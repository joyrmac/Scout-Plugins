# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.sublime-*

# PHP / Composer (dependencies are committed per-plugin only when needed)
vendor/
composer.lock

# Node (if any build tooling is added later)
node_modules/

# Logs and temp
*.log
*.tmp

# Build artifacts (zips are generated for upload, not tracked)
*.zip
